import React from "react";


// Styling
import "../components/styles/resident.css"


const Resident = (props) => {
    return (
            <div className="container">
               test
            </div>
        );
    };

    export default Resident;
